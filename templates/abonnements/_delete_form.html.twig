<form method="post" action="{{ path('app_abonnements_delete', {'id': abonnement.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cet abonnement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ abonnement.id) }}">
    <button class="btn btn-primary py-3 px-5 mt-2">Supprimer</button>
</form>
